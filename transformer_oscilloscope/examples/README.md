# Transformer Oscilloscope — Examples

- `trace_sample.jsonl`: tiny illustrative trace (2 records) showing the schema fields produced by `transformer_oscilloscope/cli.py trace`.
- Panel used in tests: `data/prompts_observability_panel_2026-03-07.jsonl` (hallucination/anchored/reasoning prompts).

Note: real traces from experiments are kept untracked to avoid repo bloat; regenerate locally with the commands in the main README or oscilloscope README.
